export enum OrderingDirection {
  DESCENDING = "desc",
  ASCENDING = "asc",
}

export enum RentalStatus {
  RETURNED = "Returned",
  EXPIRED = "Expired",
  PICKED_UP = "Picked-up",
  OVERDUE = "Overdue",
  RESERVED = "Reserved",
}
